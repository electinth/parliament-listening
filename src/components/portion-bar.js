import React from 'react'

// @todo #2 implement portionbar
const PortionBar = ({
    leftLabel, rightLabel,
    leftDuration, rightDuration,
    leftColor, rightColor
  }) => {
  return <div>

  </div>
}

export default PortionBar